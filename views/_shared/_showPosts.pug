mixin showPosts()
    script(src = '/static/js/showPosts.js')
    div#posts-container
        each comment in context.comments
            div#user-posts-wrapper
                div.post
                    | Topic : 
                    =comment.topic
                div.post
                    | Post :      
                    =comment.content
                div.post
                    | Date :
                    =comment.date